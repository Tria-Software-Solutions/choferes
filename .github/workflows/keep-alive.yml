name: Keep Backend Alive

on:
  schedule:
    - cron: '*/5 * * * *'  # Se ejecuta cada 5 minutos

jobs:
  keep_alive:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Backend
        run: curl -s https://choferesdealquiler.onrender.com/health

{
  "name": "choferes",
  "private": true,
  "scripts": {
    "lint": "npm --prefix api run lint && npm --prefix ui run lint",
    "format": "npm --prefix api run format && npm --prefix ui run format",
    "prepare": "husky install"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.1.2"
  },
  "lint-staged": {
    "**/*.{js,jsx,ts,tsx}": [
      "eslint --fix",
      "git add"
    ]
  }
}
